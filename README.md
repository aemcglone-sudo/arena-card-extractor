# arena-card-extractor
Extract Magic: The Gathering card names from photos using Claude Vision — handles images with multiple cards. Drop photos in a folder, run the script, get a clean decklist-ready list (1 Card Name per line) as TXT or CSV. Includes an offline Tesseract OCR fallback for use without an API key.
